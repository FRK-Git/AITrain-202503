import { LightningElement,api } from 'lwc';
import { NavigationMixin } from 'lightning/navigation';

export default class QuoteNewAndEdit extends NavigationMixin(LightningElement) {
    // @api recordId;
    // @api type;

    // renderedCallback() {
    //     console.log('QuoteNewAndEditLWC type:' + this.type);
    // }
}